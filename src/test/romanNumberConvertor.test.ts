describe('convert an integer to roman number', () => {
    it("an integer of 1 returns 'I'", () => {
        expect(romanNumberConvertor(1)).toBe('I');
    })
})

function romanNumberConvertor(number: number) {
    return undefined;
}
